cmake_minimum_required(VERSION 3.5)
project(homeKeeper)

SET(EXECUTABLE_OUTPUT_PATH $ENV{HOME}/github/homeKeeper/bin)
SET(LIBRARY_OUTPUT_PATH $ENV{HOME}/github/homeKeeper/lib)

add_subdirectory(homeKeeper)




